# Review

Write the draft first, then run the `craft:evaluate` skill on its path. 

The first audit covers the whole draft. After the human asks for changes or edits the file, pass only the sections that changed since the last audit.

Anything refuted means fix the draft and audit again. 

Only a clean draft goes to the human, with `AskUserQuestion`.
