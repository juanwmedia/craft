# Ask

Facts are your job and decisions are the human's: if the code answers a question, read the code and never ask it.

Ask every question that can be answered now in one numbered round, each with your recommended answer, then wait. 

Use `AskUserQuestion` for real forks between options.
